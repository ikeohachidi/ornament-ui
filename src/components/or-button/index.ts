import OrButton from './or-button.vue';

export type ButtonOptions = {
    borderRadius: number
    primary: string
    secondary: string
    disabled: string
    textColor: string
    padding: string 
}

export default OrButton;